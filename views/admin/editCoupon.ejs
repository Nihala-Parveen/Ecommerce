<%- include('../layouts/adminhomeHeader.ejs') %>

<div id="page-wrapper">
    <div class="main-page">
        <div class="forms">  
            <ol class="breadcrumb">
                <li><a href="/coupons">Coupons</a></li>
                <li class="active">Edit Coupon</li>
            </ol>    
            <div class=" form-grids row form-grids-right">
                <div class="widget-shadow " data-example-id="basic-forms"> 
                    <div class="form-title">
                        <h4>Edit Coupon</h4>
                    </div>
                    <div class="form-body">
                        <form class="form-horizontal" action="/editCoupon" method="POST"> 
                            <input type="hidden" name="id" value="<%= couponData._id %>">
                            <div class="form-group"> 
                                <label for="catname" class="col-sm-2 control-label">Coupon Code</label> 
                                <div class="col-sm-9"> <input type="text" class="form-control" name="code" required value="<%= couponData.couponCode %>">
                                    <% if(errors && errors.code ) { %>
                                        <span class="text-danger"><%= errors.code %></span>
                                    <% } %></div> 
                            </div> 
                            <div class="form-group"> 
                                <label for="catname" class="col-sm-2 control-label">Coupon Description</label> 
                                <div class="col-sm-9"> <input type="text" class="form-control" name="des" required value="<%= couponData.description %>">
                                    <% if(errors && errors.description ) { %>
                                        <span class="text-danger"><%= errors.description %></span>
                                    <% } %></div> 
                            </div>
                            <div class="form-group"> 
                                <label for="catname" class="col-sm-2 control-label">Discount Amount</label> 
                                <div class="col-sm-9"> <input type="number" class="form-control" name="discount" required value="<%= couponData.discountAmount %>">
                                    <% if(errors && errors.dis ) { %>
                                        <span class="text-danger"><%= errors.dis %></span>
                                    <% } %></div> 
                            </div>
                            <div class="form-group"> 
                                <label for="catname" class="col-sm-2 control-label">Minimum Amount</label> 
                                <div class="col-sm-9"> <input type="number" class="form-control" name="amount" required value="<%= couponData.minAmount %>">
                                    <% if(errors && errors.min ) { %>
                                        <span class="text-danger"><%= errors.min %></span>
                                    <% } %></div> 
                            </div>
                            <div class="form-group"> 
                                <label for="catname" class="col-sm-2 control-label">Expiry Date</label> 
                                <div class="col-sm-9"> <input type="date" class="form-control" name="date" required value="<%= couponData.expiryDate.toISOString().split('T')[0] %>">
                                    <% if(errors && errors.date ) { %>
                                        <span class="text-danger"><%= errors.date %></span>
                                    <% } %></div> 
                            </div>
                            <div class="col-sm-offset-2"> 
                                <button type="submit" class="btn btn-default">Edit Coupon</button> 
                            </div> 
                        </form> 
                        <%
                            if(typeof message !== 'undefined'){
                            %>
                            <p class="text-danger text-center mt-3"><%= message %></p>
                            <%
                            }
                        %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<%- include('../layouts/adminFooter.ejs') %>